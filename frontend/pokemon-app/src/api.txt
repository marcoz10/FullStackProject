How to make many api calls asynchronously 


const promises = []

for (let i = 1; i <= 150; i++){
    const url = ...${i}
    promises.push(fetch(url).then(res => res.json()))
}

Promise.all(promises).then(results) => {
    // do whatever with results
}